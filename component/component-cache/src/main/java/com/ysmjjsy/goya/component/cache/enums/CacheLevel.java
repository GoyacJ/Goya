package com.ysmjjsy.goya.component.cache.enums;

/**
 * 缓存级别枚举
 *
 * <p>定义缓存使用的级别模式，支持单级或多级缓存。
 *
 * <p><b>级别说明：</b>
 * <ul>
 *   <li><b>L1_ONLY</b>：仅使用本地缓存（L1），适用于单机场景或对一致性要求不高的场景</li>
 *   <li><b>L2_ONLY</b>：仅使用远程缓存（L2），适用于分布式场景但不需要本地缓存加速</li>
 *   <li><b>L1_L2</b>：使用多级缓存（L1 + L2），提供最佳性能和一致性平衡</li>
 * </ul>
 *
 * <p><b>使用场景：</b>
 * <ul>
 *   <li>L1_ONLY：单机应用、开发环境、测试环境</li>
 *   <li>L2_ONLY：分布式应用、需要跨节点共享数据</li>
 *   <li>L1_L2：生产环境、高并发场景、需要最佳性能</li>
 * </ul>
 *
 * @author goya
 * @since 2025/12/26 16:19
 */
public enum CacheLevel {

    /**
     * 仅使用本地缓存（L1）
     *
     * <p>只使用本地缓存，不访问远程缓存。
     * 适用于单机场景或对一致性要求不高的场景。
     */
    L1_ONLY,

    /**
     * 仅使用远程缓存（L2）
     *
     * <p>只使用远程缓存，不访问本地缓存。
     * 适用于分布式场景但不需要本地缓存加速。
     */
    L2_ONLY,

    /**
     * 使用多级缓存（L1 + L2）
     *
     * <p>使用本地缓存和远程缓存的多级缓存架构。
     * 提供最佳性能和一致性平衡，是生产环境的推荐配置。
     */
    L1_L2
}
