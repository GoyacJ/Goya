# 编码规范

## Java 与 Spring
- 使用 Java 25 语法与 Spring Boot 4 生态。
- 业务入口保持标准签名：`public static void main(String[] args)`。

## 日志与注释
- 日志需可追踪上下文，避免无语义输出。
- 注释描述“为什么”，避免重复代码字面含义。

## 配置约束
- 任何密码、密钥、令牌必须通过环境变量注入。
- 禁止提交明文敏感信息。

## Maven 约束
- 子模块依赖应由父 POM 统一管理版本。
- 禁止直接依赖聚合模块。
