# 无测试策略

## 策略声明
当前仓库采用“无测试文件”策略，不新增任何 `src/test/**` 内容。

## 原则
- 不新增测试代码。
- 不新增测试依赖。
- 不将 `mvn test` 作为提交门槛。

## 质量保障替代方案
- 以模块级 `validate` 与全仓 `compile`（`-DskipTests`）作为基础校验。
- 通过文档、配置与构建一致性保证可维护性。
- 变更后补齐 `docs/progress/changelog.md` 记录，确保可追溯。
